name: Github Actions学习与测试

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  schedule:
    - cron: '0 2 * * *'

jobs:
  ibm-cloud-restart:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Update $ Upgrade
      run: |
        #sudo apt update
        #sudo apt upgrade
        sudo apt install axel
        
    - name: Superbench
      run: |
        #wget https://raw.githubusercontent.com/oooldking/script/master/superbench.sh
        #sudo bash superbench.sh
        
    - name: Install Webbench and Test website
      run: |
        #wget http://18.162.116.199:3389/1GB.zip
        sudo apt install exuberant-ctags
        wget http://home.tiscali.cz/~cz210552/distfiles/webbench-1.5.tar.gz
        tar zxvf webbench-1.5.tar.gz 
        cd webbench-1.5/
        sudo make
        sudo make install
        ulimit -n 65535
        sudo webbench -c 4000 -t 3600 http://asglsj.com
        

        
